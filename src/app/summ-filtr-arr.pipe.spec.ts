import { SummFiltrArrPipe } from './summ-filtr-arr.pipe';

describe('SummFiltrArrPipe', () => {
  it('create an instance', () => {
    const pipe = new SummFiltrArrPipe();
    expect(pipe).toBeTruthy();
  });
});
